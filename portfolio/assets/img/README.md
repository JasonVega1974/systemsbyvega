# Project thumbnails

Drop one image per project here. The filename must match the `thumb` field in
`assets/js/projects.js`.

Expected files:

| File                       | Project                       |
|----------------------------|-------------------------------|
| `systemsbyvega.png`        | Systems by Vega               |
| `kingdom-creatives.png`    | Kingdom Creatives             |
| `yourlifecc.png`           | YourLife CC                   |
| `faith-journey.png`        | YourLife CC — Faith Journey   |
| `estatesalebiz.png`        | Estate Sale Biz               |
| `og-cover.png`             | Social share image (1200×630) |

**Guidance (Phase 4):** screenshot each live site at a consistent size — a clean
16:9 crop (e.g. 1280×720) works well. Until a real image is added, each card shows
a generated initials tile, so the site looks finished with zero images.
